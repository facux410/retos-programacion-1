const rebanadas = parseInt(
  prompt("cuantas rebanadas de pizza trajiste bro?", "")
);
const consumo = parseInt(
  prompt("cuantas rebanadas de pizza te comiste wachin ", "")
);
const resultado = rebanadas - consumo;

alert(`te quedan ${resultado} de rebanadas gordito`);
